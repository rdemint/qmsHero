<UserControl x:Class="QmsDoc.Controls.FilePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="FilePickerRoot">
    <StackPanel
        Name="FilePickerPanel"
        Orientation="Horizontal"
        DataContext="{Binding ElementName=FilePickerRoot}"
        >
        <Button
            Name="FilePickerButton"
            Content="{Binding Path=DisplayName}"
            MinWidth="0"
            Command="{Binding Path=OpenFileDialog}"
            />
        <TextBox MinWidth="200" Text="{Binding Path=ControlValue}"/>
    </StackPanel>
</UserControl>
