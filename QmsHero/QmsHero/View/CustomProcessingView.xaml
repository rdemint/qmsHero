<Page x:Class="QmsHero.View.CustomProcessingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QmsHero.View"
      xmlns:core ="clr-namespace:QmsDoc.Core;assembly=QmsDoc"
      xmlns:controls ="clr-namespace:LadderControls;assembly=LadderControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{
            Binding CustomProcessingViewModel, Source={StaticResource ViewModelLocator}}"
      ShowsNavigationUI="False"
      >
    <StackPanel
        Margin="20"
        Background="AliceBlue">
        <controls:QFolderPicker QDisplay="Project Dir" QOutput="{Binding Path=OriginalDirPath}"/>
        <controls:QCheckFilePicker QDisplay="Logo" QOutput="{Binding Path=DocEdit.DocHeader.LogoPath.Value}"/>
        <controls:QCheckTextBox QDisplay="Effective Date" QOutput="{Binding Path=DocEdit.DocHeader.EffectiveDate.Value}"/>
        <controls:QCheckTextBox QDisplay="Revision" QOutput="{Binding Path=DocEdit.DocHeader.Revision.Value}"/>
        <Button 
            Command="{Binding ProcessFilesCommand}" 
            CommandParameter="{Binding ElementName=ProjectDirElement, Path=QOutput}"
            Content="Process Files"/>
    </StackPanel>
</Page>
